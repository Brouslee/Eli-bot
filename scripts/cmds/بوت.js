module.exports = {
    config: {
        name: "بوت",
        version: "1.1",
        author: " ",
        countDown: 5,
        role: 0,
        shortDescription: "رد بوت",
        longDescription: "🌝",
        category: "أدوات",
    },
onStart: async function(){}, 
onChat: async function({
    event,
    message,
    getLang
}) {
    if (event.body && event.body.toLowerCase() == "بوت") return message.reply("\nموجود.. انت شتريد؟")

    if (event.body && event.body.toLowerCase() == "سعيدة") return message.reply("\nقرة عيني 😻🤍🤍")                                                                      

    if (event.body && event.body.toLowerCase() == "تحبك") return message.reply("\nحبك برص")

    if (event.body && event.body.toLowerCase() == "حمود") return message.reply("\nشتبي منه؟")

    if (event.body && event.body.toLowerCase() == "حمادي") return message.reply("\nلا تزعج المطور..")

    if (event.body && event.body.toLowerCase() == "السلام عليكم") return message.reply("\nوعليكم سلام")

    if (event.body && event.body.toLowerCase() == "المطور") return message.reply("\n همم ذا حسابه: \n | https://m.me/ukidn")

    if (event.body && event.body.toLowerCase() == "نعم") return message.reply("\n نعامة ترفسك")

    if (event.body && event.body.toLowerCase() == "يب") return message.reply("\n روح نام")

    if (event.body && event.body.toLowerCase() == "صباح الخير") return message.reply("\n صباح النور")

    if (event.body && event.body.toLowerCase() == "ثباحو") return message.reply("\nثبحورددد")

    if (event.body && event.body.toLowerCase() == "انا") return message.reply("\n شفيك انت؟")

    if (event.body && event.body.toLowerCase() == "انت") return message.reply("\n مين هو")

    if (event.body && event.body.toLowerCase() == "💔") return message.reply("\nشفيه قلبك مكسور")

    if (event.body && event.body.toLowerCase() == "👽") return message.reply("\n هل انت فضائي؟")

    if (event.body && event.body.toLowerCase() == "🦍") return message.reply("\nهل انت غوريلا؟")

    if (event.body && event.body.toLowerCase() == "🗿") return message.reply("\n:-:؟")

    if (event.body && event.body.toLowerCase() == "سينرا") return message.reply("\n دير بالك من تزعجها")

    if (event.body && event.body.toLowerCase() == "ون بيس عمك") 
return message.reply("\nاتفق")

    if (event.body && event.body.toLowerCase() == "بوتة") return message.reply("\n لا تزعجني")

    if (event.body && event.body.toLowerCase() == "بوت") return message.reply("\nوش يعيون")

    if (event.body && event.body.toLowerCase() == "بحبك") return message.reply("\nحبك برص")

    if (event.body && event.body.toLowerCase() == "توته") return message.reply("\n 😳وي")

    if (event.body && event.body.toLowerCase() == "حمودي") return message.reply("\nلا تزعجو مطوري😾💢")

    if (event.body && event.body.toLowerCase() == "سلام عليكم") return message.reply("\nوعليكم سلام")

    if (event.body && event.body.toLowerCase() =="💔") 
return message.reply("\nلماذا نحن هنا؟")

    if (event.body && event.body.toLowerCase() =="لا") 
return message.reply("\nكذاب")

    if (event.body && event.body.toLowerCase() =="😐") 
return message.reply("\n😐")

    if (event.body && event.body.toLowerCase() =="قطوتي") 
return message.reply("\nمياووو  ")

    if (event.body && event.body.toLowerCase() =="كيفك") 
return message.reply("\nالحمدلله مشتاقين")

    if (event.body && event.body.toLowerCase() =="ليش") 
return message.reply("\nلانك حريمة")

    if (event.body && event.body.toLowerCase() =="🙂") 
return message.reply("\n😾💢ايموجي مستفز")

    if (event.body && event.body.toLowerCase() =="اي") 
return message.reply("\nكذب")

    if (event.body && event.body.toLowerCase() =="موسى") 
return message.reply("\nموسى كوسى")

    if (event.body && event.body.toLowerCase() =="انجب") 
return message.reply("\nعيب")

    if (event.body && event.body.toLowerCase() =="هلا") return message.reply("\nنورتت")

    if (event.body && event.body.toLowerCase() =="منور") 
return message.reply("\n بوجودك عيوني ")

    if (event.body && event.body.toLowerCase() =="منورة") 
return message.reply("\nبوجودك عيوني")

    if (event.body && event.body.toLowerCase() =="احمد") 
return message.reply("\n احمد محسن")

    if (event.body && event.body.toLowerCase() == "طيب") return message.reply("\n 😽")

    if (event.body && event.body.toLowerCase() == "احبك") return message.reply("\nمي طو برو نرتبط؟")

    if (event.body && event.body.toLowerCase() == "كيفكم") return message.reply("\nالحمد الله مشتاقين ")

    if (event.body && event.body.toLowerCase() == "هجوم الضفادع") return message.reply("\n🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸🐸⚔️⚔️")

    if (event.body && event.body.toLowerCase() == "عطوني شكلاته") return message.reply("\n🍫🍫🍫🍫🍫🍫")

    if (event.body && event.body.toLowerCase() == "ايوا") return message.reply("\nايواااااااا")

    if (event.body && event.body.toLowerCase() == "طز") return message.reply("\nفيك")

    if (event.body && event.body.toLowerCase() == "ايمان") return message.reply("\nهل انت نعسان؟")

    if (event.body && event.body.toLowerCase() == "حصل") return message.reply("\nهل انت بصل🌰؟")

    if (event.body && event.body.toLowerCase() == "بانكاي") return message.reply("\n عيب")

    if (event.body && event.body.toLowerCase() == "🥺") return message.reply("\nعيب!!")

    if (event.body && event.body.toLowerCase() == "😁") return message.reply("\nلماذا انت بمتسم؟")

    if (event.body && event.body.toLowerCase() == ":-:") return message.reply("\n 🫦")

    if (event.body && event.body.toLowerCase() == "'-'") return message.reply("\n 🫦")

    if (event.body && event.body.toLowerCase() == "بتول") return message.reply("\n 😡انتي طالق بلاربعة")

		if (event.body.toLowerCase().indexOf("يس") !== -1) return api.setMessageReaction("\n يس؟؟؟", event.messageID,event.threadID)

    if (event.body && event.body.toLowerCase() == "تفضل") return message.reply("\n لا تزحف")

    if (event.body && event.body.toLowerCase() == "تفضلي") return message.reply("\nلا تزحف")

    if (event.body && event.body.toLowerCase() == "كتكوتة") return message.reply("\nصوص")

    if (event.body && event.body.toLowerCase() == "خاص") return message.reply("\n ؟ الخاص للرخاص")

    if (event.body && event.body.toLowerCase() == "كيوت") return message.reply("\n انقلع")

    if (event.body && event.body.toLowerCase() == "مح") return message.reply("\nعخدودكك")

    if (event.body && event.body.toLowerCase() == "طرد")
return message.reply("\nما لحق يقول كلمته الأخيرة")

    if (event.body && event.body.toLowerCase() == "نفخ")
return message.reply("\nما لحق ههههه يقول كلمتهالأخيرة" )

}
};
